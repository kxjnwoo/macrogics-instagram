# Editorial Minimalism

색상·규격은 `src/tokens.ts`, 폰트 로딩은 `src/fonts.ts`, 혼합 서체는 `src/typography.ts`, 공통 화면은 `src/components/DesignFrame.tsx`에 정의한다. 플랫폼 기하 정보는 `src/platform-layout.ts`에 있다.

## 색상과 브랜드

Ivory `#F3F0E7`, ink `#242A27`, muted `#69736C`, line `#C5C8BC`, green `#446557`, rust `#985F4C`. 머리말은 기존 50px 심볼과 Prociono 워드마크 사이에 16px 간격을 둔다. 워드마크는 47px이다. 심볼 SVG는 기존 원형 로고를 바탕으로 정확한 녹색 원과 흰색 m을 표시한다. `public/logo.png`, `logo.ico`, 기존 변형과 `public/branding/` 원본을 유지한다.

## 서체

모든 서체는 `public/fonts/`의 로컬 파일을 사용한다.

| 역할 | 서체 | 자간 |
|---|---|---|
| Macrogics 워드마크 | Prociono Regular | −2% |
| 날짜 | Instrument Serif Regular | normal |
| 한글 제목 | Noto Serif KR SemiBold 600 | −9% |
| 한글 본문 | Pretendard Regular 400 | −2% |
| 본문 영문·숫자 | Instrument Serif Regular | normal |
| 한글 자막 | Pretendard SemiBold 600 | −1.5% |
| 자막 영문·숫자 | PP Editorial New Regular | −1.5% |
| 한글 인용문 | ChosunNm Regular | −2% |
| 차트 영문·숫자·기호 | Instrument Serif Regular | +1% |
| 차트 한글 | Pretendard Regular | −2% |

제목 86px, 본문 64px, 차트 설명·요약 56px, 커버 본문 76px, 독립 인용문 66px, 자막 54px, 날짜 36px, 출처 25px를 사용한다. 시각 요소의 보조 문구는 관계·통계 설명 60px, 아이콘 라벨·이름 48px, 역할 28px, 인물 인용문 50px, 통계 단위 56px다. 통계의 주 숫자, 차트 축·제목은 각 컴포넌트의 독립 역할을 유지한다. 보조 문구는 ink, 인물 역할은 muted, 차트 변화 값은 해당 시리즈 색상이다.

`MixedText`와 `ChartText`는 실제 글자 크기에서 자간을 계산한다. 64px 한글 본문의 −2%는 −1.28px이다. 작은 부모 요소의 em 자간을 상속하지 않는다. 본문/자막 자간이 차트에 적용되지 않도록 역할을 구분한다. `20bp는` 같은 숫자·단위·조사와 한글 단어, `수 있다` / `수 없다`는 중간에서 나누지 않는다. 자막의 명시된 줄바꿈을 보존하고 허용된 공백에서만 균형을 조절한다.

ChosunNm.woff2는 기존 TTF의 윤곽과 글리프를 유지하면서 Chromium이 읽을 수 있도록 vhea 버전을 보정한 파일이다. 기존 TTF도 유지한다.

## 화면과 안전 영역

기준 화면은 1080×1920, 30fps다. 위치는 공유 토큰을 사용한다.

| 영역 | 좌표와 크기 |
|---|---|
| 본문 열 | x144–888, 폭744 |
| 머리말·구분선 | x144–936, 폭792, y280 시작 |
| 제목 | y408 기준; 두 줄의 서체 메트릭 포함 y396–660 |
| 중앙 시각 영역 | y700 시작, 높이620 |
| 차트 플롯 | 높이420 |
| 차트 라벨 | 높이44 + 간격16 |
| 차트 아래 본문 | 간격24, 양옆 추가16px |
| 자막·팔로우 | x176–904, 폭728, y1396–1568 |
| 출처·주석 | x350–896, y1620–1690, 오른쪽 정렬, 줄높이1.35 |

머리말과 자막은 x540을 중심으로 한다. 중앙 열 오른쪽은 x888, 출처 오른쪽은 x896이다. 캡처에서 관찰된 액션 레일은 x920에서 시작하며 스트레스 프로필은 x912까지 여유를 둔다. 출처를 화면 왼쪽 끝이나 하단 계정 블록으로 옮기지 않는다. 하단 날짜 배지, 자막 구분선, 진행 막대는 없다.

캡처 기반 프로필은 1206×2622 화면에서 약 1206×2372의 영상 뷰포트와 폭 맞춤 대비 약 10.6% 확대를 반영한다. 소스→화면 배율은 약1.2355이며 양쪽 약53px가 잘린다. 이는 특정 캡처에 따른 값으로 모든 Instagram 화면의 공통 규격은 아니다. observed/stress 두 프로필의 크롭·상단 컨트롤·오른쪽 레일·하단 컨트롤 경계는 `platform-layout.ts`가 정의한다.

제목은 약 두 줄, 자막은 최대 두 줄을 위한 공간을 갖는다. 자막은 흰색 불투명 박스에 세로12px·가로24px 패딩을 두고 가운데 정렬한다. 본문과 인용문은 `bodyInset`을 적용한다. 차트 축과 제목은 본문 열의 기본 정렬을 유지한다. 작은 글씨로 축소해 공간을 맞추지 않는다.

## 차트와 시각 컴포넌트

라인·영역·막대·성과·지수·캔들·수익률곡선·섹터 밸류에이션 컴포넌트를 유지한다. 막대는 0을 기준으로 하며 양수·음수 방향을 보존한다. 지수 비교는 기준값100을 라벨로 표시하고 수익률곡선은 만기 간격을 사용한다. 차트 제목은 오른쪽 정렬의 시리즈 이름 + `(단위: …)`이며 날짜는 x축에 둔다. 시리즈 요약에는 이름과 색상 표식 또는 원형 회사 로고가 붙는다.

원본 관측값의 날짜와 null을 유지한다. 날짜 축은 빈 날짜를 압축하지 않는다. 확인된 휴장 구간의 선 연결과 미확인 결측의 구분은 `chart-observations.ts`에 있다. NumberCounter는 표시 애니메이션이다.

차트는 프레임에 따라 그려진다. 선 머리 반지름은 6px이며 그리기 완료 후 끝점 파동은 45프레임 주기, 30프레임 활성 상태다. 진입은 18프레임과 12px 오프셋을 사용한다. 강한 줌·휩, 장식적 글로우, 브라우저 시간에 의존하는 애니메이션은 사용하지 않는다. 얇은 구분선과 여백을 유지하며 3D 아이콘은 차분한 재질·조명·크기로 표현한다.

중앙 영역에는 인물, 관계, 타일, 수치, 질문·문장, 복합 레이아웃을 배치할 수 있다. 인물과 텍스트의 간격은48px, 인물 이미지 기본 크기는280×500px다. 이름·역할·인용문의 크기는 위의 역할을 따른다. Eyebrow는 입력이 없으면 표시되지 않으며 영문은 Instrument Serif, 한글은 Chosun을 쓴다.

`SceneVisual`은 표시할 시각 요소와 주어진 큐 시각만 갖는다. `VisualContext`는 차트 데이터·통계 값·시각 자산·밸류에이션 행을 전달한다. 복합 요소는 중앙 영역 내 정규화한 box를 사용하며 글자는 CSS scale로 축소하지 않는다. 이미지 focus는 cover 크롭의 중심점을 지정한다. 생성 삽화는 자산의 `origin: generated`로 작은 표시를 붙인다.

요소는 cueIndex/endCueIndex/emphasisCueIndex로 주어진 큐에 등장·종료·강조될 수 있다. enter는 none/fade/rise, exit는 none/fade, emphasis는 none/outline/underline이다. rise는 box 내부12px 여백에서 움직인다. 같은 공간은 서로 다른 시점에 사용할 수 있다. 동시에 표시하는 요소는 겹치지 않도록 배치한다.

도식 노드는 내부 정규화 box, 라벨, 보조 라벨, 선택적 이미지와 plain/card 형태를 갖는다. 간선은 sequence의 화살표, association의 선, conditional의 점선 화살표다. 간선은 지정한 큐에서 한 번 그려지고 유지된다. 연결점은 노드 밖12px에 두며 top/right/bottom/left를 지정할 수 있다. 문구·이미지의 실제 크기를 유지한다.

## 마지막 팔로우 카드

`FollowCard`는 기존 96프레임(3.2초) 카드와 클릭음을 포함한다. 외부에서 시작 프레임을 지정할 수 있다. 카드 자체에는 내레이션이 없다.

기존 중앙 시각과 머리말 위에 x176–904, y1408의 728×146px 유리 캡슐이 표시된다. 모서리는73px, 바탕은 `rgba(255,255,255,0.70)`, blur20px와 차분한 반투명 그라디언트다. 질문 `도움되는 투자 정보를 계속 받으려면?`는42px Pretendard로 한 줄 표시 후 사라진다. 이어서82px 심볼,40px `매크로직스`,25px `@macrogics`,166×60px 파란 팔로우 버튼이 나타난다. 프로필·버튼의 캡슐 양끝 여백은 약45px다.

커서는 프레임86에 한 번 클릭하고 팔로잉 상태가 남는다. 원본 클릭음은 `public/audio/follow-click.wav`이며 소스 피크−6dB, 재생 볼륨0.4다. 파일의 한 프레임 프리롤을 보정해 버튼 눌림과 소리를 맞춘다. 기존 문구·애니메이션·로고·음원을 유지한다.
